# tornado_setup
easily set up tornado app with templates
# usage
source install.sh
tornado_setup <app-name>
# directory-tree
```
.
├── server.py
├── static
│   └── css
│       └── style.css
├── templates
│   └── index.html
└── venv
     .
     .
     .  
```
